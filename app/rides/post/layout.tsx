import React from 'react';

/**
 * Layout for the "post a ride" section.
 */
export default async function ShareAvailabilityLayout({
  children,
}: {
  readonly children: React.ReactNode;
}) {
  return <>{children}</>;
}
