import React from 'react';

/**
 * Layout for the rides section.
 * Wraps ride-related pages.
 */
export default async function RidesLayout({ children }: { readonly children: React.ReactNode }) {
  return <>{children}</>;
}
