import React from 'react';

/**
 * Layout for the messages section.
 */
export default async function MessagesLayout({ children }: { readonly children: React.ReactNode }) {
  return <>{children}</>;
}
